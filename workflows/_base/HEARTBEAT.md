# Heartbeat Tasks

These tasks run periodically based on the `[heartbeat]` interval.

## Template

Replace these with your workflow-specific periodic checks:

- [ ] Check system health
- [ ] Review pending items
- [ ] Send status update if anything changed
